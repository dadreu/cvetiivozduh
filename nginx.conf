server {
    listen 80;
    server_name flowershop.local; # Заменить на реальный домен
    
    # Редирект на HTTPS
    # return 301 https://$host$request_uri;
}

# Раскомментировать при наличии SSL-сертификатов
# server {
#     listen 443 ssl http2;
#     server_name flowershop.local;
#     
#     ssl_certificate /etc/letsencrypt/live/flowershop.local/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flowershop.local/privkey.pem;

    # Security Headers
#     add_header X-Frame-Options "SAMEORIGIN";
#     add_header X-Content-Type-Options "nosniff";
#     add_header X-XSS-Protection "1; mode=block";

    # Статика Vue 3 (Пререндеринг)
#     location / {
#         root /var/www/flowershop/frontend/dist;
#         try_files $uri $uri/ /index.html;
#     }

    # Раздача загруженных изображений
#     location /uploads/ {
#         alias /var/www/flowershop/uploads/;
#         expires 30d;
#         add_header Cache-Control "public";
#     }

    # Reverse Proxy для Kestrel (.NET)
#     location /api/ {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection keep-alive;
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
